name: Test on Windows
on: [push]
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run test suite
        shell: pwsh
        run: |
          .\Test-CodexBar.ps1 -Verbose
